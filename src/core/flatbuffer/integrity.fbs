namespace NovaX.Integrity;

table FileEntry {
  path: string (key);
  hash: [ubyte];
  size: uint;
}

table Manifest {
  timestamp: ulong;
  algorithm: string;
  files: [FileEntry];
  ignore_hash: [string];
}

root_type Manifest;
